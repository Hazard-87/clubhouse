import clsx from 'clsx';

import styles from './Avatar.module.scss';

export const Avatar = () => {
  return (
    <div
      style={{ width, height, backgroundImage: `url('')` }}
      className={clsx(styles.avatar)}></div>
  );
};
